worker_processes 1;
events { worker_connections 1024; }

http {
    server {
        listen <%= ENV["PORT"] %>;

        location / {
            root <%= ENV["HEROKU_APP_DIR"] %>/public;
            index index.html;
            try_files $uri $uri/ /index.html;
        }
    }
}
